apiVersion: portal.solo.io/v1alpha1
kind: ApiDoc
metadata:
  name: httpbin
  namespace: kgateway-system
  labels:
    product: httpbin
spec:
  source:
    url:
      endpoint: https://raw.githubusercontent.com/solo-io/solo-blog/main/zero-to-gateway/httpbin-openapi.json
  servedBy:
    name: httpbin
    namespace: kgateway-system
    port: 8000